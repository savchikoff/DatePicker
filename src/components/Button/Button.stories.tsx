import Button from ".";

export default {
    title: 'Calendar/Button',
    component: Button,
    argTypes: {
        label: {
            name: "Button label"
        }
    }
};

export const BasicButton = {
    args: {
        label: "Button"
    }
};